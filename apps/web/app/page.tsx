import { SearchWorkbench } from "@/components/search-workbench";
export default function HomePage() {
  return <main><section className="hero"><p className="eyebrow">MULTIMODAL MATERIAL INTELLIGENCE</p>
    <h1>Find the material you can <span>picture.</span></h1>
    <p className="lede">Describe a surface, upload a reference, or combine both. Search across brand, series, finish, performance, and visual character.</p></section>
    <SearchWorkbench /></main>;
}
